to build a jar and run it: ./mvnw package && java -jar target/spring-boot-docker-0.0.1-SNAPSHOT.jar

to limit a container's cpu we can use the command docker top <containerId> which gives us the processes running inside the container, then we can run command sudo cpulimit -l 50 -p <processId>
